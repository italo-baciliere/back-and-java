# POSTGRESQL -------------------------------------------------------------

## Application port
server.port=8081

## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.url=jdbc:postgresql://localhost:5432/dev
spring.datasource.username=admin
spring.datasource.password=example

## Default Schema
spring.flyway.schemas=users
spring.jpa.properties.hibernate.default_schema=users

## Configurações sql
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update

# Retornar stacktracer nas requisições REST
server.error.include-stacktrace=NEVER




# MYSQL -------------------------------------------------------------------

# # Exibir os logs dos comandos sql
# spring.jpa.show-sql=true
# #  informações com quebras de linha
# spring.jpa.properties.hibernate.format_sql=true

# spring.jpa.hibernate.ddl-auto=update
# spring.datasource.url=jdbc:mysql://localhost/vollmed_api
# spring.datasource.username=root
# spring.datasource.password=example

# # Customização de parâmetros
# spring.data.web.pageable.page-parameter=pagina
# spring.data.web.pageable.size-parameter=tamanho
# spring.data.web.sort-parameter=ordem
# # http://localhost:8080/medicos?pagina=2&tamanho=2&ordem=email,desc

# server.error.include-stacktrace=NEVER
# # https://docs.spring.io/spring-boot/docs/current/reference/html/application-properties.html#appendix.application-properties.server




# RESOLUÇÕES -------------------------------------------------------------------

# Introduction to Docker Compose
# https://www.baeldung.com/ops/docker-compose

# Set up a Spring Boot Application with PostgreSQ
# https://dzone.com/articles/bounty-spring-boot-and-postgresql-database

# Running Spring Boot with PostgreSQL in Docker Compose
# https://www.baeldung.com/spring-boot-postgresql-docker